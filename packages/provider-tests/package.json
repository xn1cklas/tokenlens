{
  "name": "provider-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "pnpm -w --filter tokenlens build",
    "test": "vitest --config ../../vitest.config.ts --project provider-tests",
    "test:run": "vitest run --config ../../vitest.config.ts --project provider-tests",
    "test:live": "vitest run --config ../../vitest.config.ts --project provider-tests -t smoke",
    "bench": "tsx scripts/benchmark.ts"
  },
  "devDependencies": {
    "@ai-sdk/provider": "^2.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "ai": "^5.0.51",
    "tsx": "^4.20.5"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.18",
    "@ai-sdk/gateway": "^1.0.29",
    "@ai-sdk/openai": "^2.0.35",
    "@ai-sdk/xai": "^2.0.22",
    "tokenlens": "workspace:*"
  }
}
